import type { ObjectPlugin } from 'vue';

/**
 * Ariadna UI | Services | Components | ViewerService
 *
 * ViewerService is a Vue plugin that provides Viewer-related functionality to Vue applications.
 *
 * @description ViewerService can be installed into a Vue app to enable features of {@link Viewer} component.
 */
declare const ViewerService: ObjectPlugin;

export default ViewerService;
