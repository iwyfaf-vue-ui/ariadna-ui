import type { TViewerApi } from '../../../components/media/Viewer/Viewer';

/**
 * Ariadna UI | Composables | Components | useViewer
 *
 * Provides a composable function to access the Viewer API, enabling interaction with media viewer components.
 *
 * @returns {TViewerApi} - An object implementing the Viewer API, which exposes methods and properties to control the
 * media viewer.
 */
declare function useViewer(): TViewerApi;

export default useViewer;
