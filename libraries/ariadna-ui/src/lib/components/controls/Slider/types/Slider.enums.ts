/**
 * Slider default props values.
 */
export enum ESliderPropsDefault {
  MIN = 0,
  MAX = 100,
  CSS_CLASS = 'ar-slider',
}

/**
 * Slider thumb position values.
 */
export enum EThumbPosition {
  LEFT,
  RIGHT,
}
