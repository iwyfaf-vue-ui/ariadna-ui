# Техническое задание

Необходимо полностью реализовать Vue 3 компонент Rating.vue по требованиям ниже, включая ВСЕ перечисленные в
структуре файлы.

## Структура:

```text
├── Rating                                            # Каталог с именем компонента
│   ├── core                                          # Каталог с основными функциональными возможностям элемента
│   │   ├── rating-math                               # Каталог с функционалом математических операций рейтинга
│   │   │   ├── rating-math.core.ts                   # Главный файл
│   ├── composables                                   # Каталог с описанием реактивной функциональности компонента (Vue 3)
│   │   ├── useRating                                 # Каталог с composable useRating
│   │   │   ├── useRating.ts                          # Composable useRating
│   │   │   ├── useRating.types.ts                    # Типы composable useRating
│   │   ├── useRatingEvents                           # Каталог с composable useRating
│   │   │   ├── useRatingEvents.ts                    # Composable useRatingEvents
│   │   │   ├── useRatingEvents.types.ts              # Типы composable useRatingEvents
│   ├── tests                                         # Каталог с Unit-тестами компонента
│   │   ├── core                                      # Каталог с Unit-тестами для core
│   │   │   ├── rating-math.core.test.ts              # Unit-тесты математических операций рейтинга
│   │   ├── components                                # Каталог с Unit-тестами для компонентов
│   │   │   ├── Rating.test.ts                        # Unit-тесты главного компонента
│   │   ├── composables                               # Каталог с Unit-тестами для composables
│   │   │   ├── useRating.test.ts                     # Unit-тесты composable useRating
│   │   │   ├── useRatingEvents.test.ts               # Unit-тесты composable useRatingEvents
│   ├── types                                         # Каталог с типами компонента
│   │   ├── Rating.enums.ts                           # Enums компонента со значениями props по умолчанию
│   │   ├── Rating.types.ts                           # Types компонента
│   ├── Rating.vue                                    # Основной (входной) файл элемента
│   ├── Rating.d.ts                                   # Основной файл с типами элементов
```

## Описание компонента

Компонент Rating предназначен для отображения и выбора рейтинга в виде набора звёзд (или других иконок). Пользователь
может выставить оценку, увидеть текущий рейтинг, сбросить его, а также просмотреть возможные ошибки или подсказки.

## Общие требования

- Компонент должен поддерживать SSR.
- Использование Composition API + `<script setup>`.
- Props типизация через `defineProps<T>()`.
- Slots типизация через `defineSlots<T>()`.
- Emits типизация через `defineEmits<T>()`.
- Строгое разделение логики, типов, core-функционала и тестов по соответствующим директориям.

## Функциональные требования

- Реактивный функционал должен быть реализован в composable `useRating`.
- Отображение заданного количества звёзд (по умолчанию `5`).
- Возможность выбора значения рейтинга с заданным шагом (например, `0.5`, `1`).
- Отображение текущего значения рейтинга.
- Возможность сброса значения рейтинга (если включено).
- Кастомизация отображения отдельных элементов через слоты (звёзды, значение, сброс, ошибки).
- Поддержка различных позиций отображения значения рейтинга (сверху, снизу, слева, справа от звёзд).
- Отображение ошибок валидации с анимацией.

### Props

| Prop            | Required | Type                        | Default                                                               | Description                                                                                                      |
|-----------------|----------|-----------------------------|-----------------------------------------------------------------------|------------------------------------------------------------------------------------------------------------------|
| `modelValue`    | ✓        | `number \| null`            | `0`                                                                   | Значение компонента.                                                                                             |
| `label`         | -        | `string`                    | `undefined`                                                           | Текст элемента `label`.                                                                                          |
| `id`            | -        | `string`                    | Случайно сгенерированная строка с помощью помощника Vue 3.5 `useId()` | Значение для атрибутов `id` и `for`.                                                                             |
| `starCount`     | -        | `number`                    | `ERatingPropsDefault.STAR_COUNT`                                      | Количество отображаемых звёзд.                                                                                   |
| `fillStep`      | -        | `number`                    | `ERatingPropsDefault.FILL_STEP`                                       | Значение шага заполнения рейтинговой звездочки.                                                                  |
| `singleMode`    | -        | `boolean`                   | `false`                                                               | Будет отображаться одна звездочка вместо общего количества звездочек. Работает только с prop `:readonly="true"`. |
| `size`          | -        | `TSharedPropsSize`          | `ERatingPropsDefault.SIZE`                                            | Предопределенные варианты размеров компонента.                                                                   |
| `showValue`     | -        | `boolean`                   | `false`                                                               | Отобразит текущий рейтинг в виде понятных значений. Например: `3.5/5`.                                           |
| `valuePosition` | -        | `TRatingPropsValuePosition` | `ERatingPropsDefault.VALUE_POSITION`                                  | Расположите значение относительно звезд.                                                                         |
| `reset`         | -        | `boolean`                   | `false`                                                               | Включает или отключает возможность сброса рейтинга.                                                              |
| `disabled`      | -        | `boolean`                   | `false`                                                               | Состояние компонента `disabled`.                                                                                 |
| `readonly`      | -        | `boolean`                   | `false`                                                               | Отключение режима редактирования.                                                                                |
| `valid`         | -        | `boolean`                   | `false`                                                               | Состояние компонента `valid`.                                                                                    |
| `invalid`       | -        | `boolean`                   | `false`                                                               | Состояние компонента `invalid`.                                                                                  |
| `cssClass`      | -        | `string`                    | `ERatingPropsDefault.CSS_CLASS`                                       | Переопределяет структуру CSS классов.                                                                            |
| `modifier`      | -        | `TSharedPropsModifier`      | `undefined`                                                           | Модификатор базового CSS-класса.                                                                                 |

### Slots

| Slot       | Description                                                   |
|------------|---------------------------------------------------------------|
| `active`   | Используется для отображения кастомной активной звезды.       |
| `inactive` | Используется для отображения кастомной неактивной звезды.     |
| `value`    | Используется для отображения кастомного значения рейтинга.    |
| `reset`    | Используется для отображения кнопки сброса значения рейтинга. |
| `errors`   | Используется для отображения ошибок компонента.               |

### Emits

| Event                | Payload                               | Description                                               |
|----------------------|---------------------------------------|-----------------------------------------------------------|
| `update:model-value` | `payload: TRatingProps['modelValue']` | Событие срабатывает при обновлении значения `modelValue`. |
| `focus`              | `event: Event`                        | Событие срабатывает когда компонент получает фокусировку. |

### Поддержка клавиатуры

| Key   | Function                        |
|-------|---------------------------------|
| `tab` | Перемещает фокус на звезды.     |
| `esc` | Сбрасывает значение компонента. |

## Требования к тестированию:

- Покрыть компонент unit-тестами.

## Требования к документации

- Типы должны находится в отдельном файле `useRating.d.ts`;
- Добавить TSDoc-комментарии к типам, компоненту и composable.
- Описание сценариев применения компонента.
