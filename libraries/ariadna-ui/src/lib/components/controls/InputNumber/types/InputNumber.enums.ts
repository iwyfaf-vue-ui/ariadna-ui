/**
 * InputNumber config.
 */
export enum EInputNumberConfig {
  NAME = 'InputNumber',
}

/**
 * InputNumber default props values.
 */
export enum EInputNumberPropsDefault {
  SIZE = 'medium',
  STEP = 1,
  EMPTY = '',
  CSS_CLASS = 'ar-input-number',
}

/**
 * Viewer errors.
 */
export enum EInputNumberErrors {
  V_INPUT_NUMBER_DIRECTIVE_REQUIRES_INPUT_ELEMENT = 'vInputNumber directive requires an input element.',
}
