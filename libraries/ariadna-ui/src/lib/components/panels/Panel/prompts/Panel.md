# Компонент Panel.vue

Техническое задание для компонента Panel:

Необходимо полностью реализовать Vue 3 компонент Panel.vue по требованиям ниже, включая ВСЕ перечисленные в
структуре файлы.

## Структура:

```text
├── Panel                                                  # Каталог с именем компонента
│   ├── composables                                        # Каталог с описанием реактивной функциональности компонента (Vue 3)
│   │   ├── usePanel                                       # Каталог с composable usePanel
│   │   │   ├── usePanel.ts                                # Composable usePanel
│   │   │   ├── usePanel.types.ts                          # Типы composable usePanel
│   │   ├── usePanelCollapse                               # Каталог с composable usePanelCollapse
│   │   │   ├── usePanelCollapse.ts                        # Composable usePanelCollapse
│   │   │   ├── usePanelCollapse.types.ts                  # Типы composable usePanelCollapse
│   ├── tests                                              # Каталог с Unit-тестами компонента
│   │   ├── components                                     # Каталог с Unit-тестами для компонентов
│   │   │   ├── Panel.test.ts                              # Unit-тесты главного компонента
│   │   ├── composables                                    # Каталог с Unit-тестами для composables
│   │   │   ├── usePanel.test.ts                           # Unit-тесты composable usePanel
│   │   │   ├── usePanelCollapse.test.ts                   # Unit-тесты composable usePanelCollapse
│   ├── types                                              # Каталог с типами компонента
│   │   ├── Panel.enums.ts                                 # Enums компонента
│   │   ├── Panel.types.ts                                 # Типы компонента
│   ├── Panel.vue                                          # Основной (входной) файл элемента
│   ├── Panel.d.ts                                         # Основной файл с типами элементов
```

## Описание компонента

Компонент Panel предназначен для группировки контента с возможностью сворачивать и разворачивать его содержимое.

## Общие требования

- Компонент должен поддерживать SSR.
- Использование Composition API + `<script setup>`.
- Props типизация через `defineProps<T>()`.
- Slots типизация через `defineSlots<T>()`.
- Emits типизация через `defineEmits<T>()`.
- Строгое разделение логики, типов, core-функционала и тестов по соответствующим директориям.

## Функциональные требования

### Визуальный вид

- Если слот header не задан, используется значение props `header`.
- Должна присутствовать гибкая настройка анимации сворачивания и разворачивания.

### Сворачиваемость / Разворачивание

- Если props `toggleable` = `true`, панель можно свернуть или развернуть.
- При нажатии на кнопку элемент из слота `toggleButton` содержимое панели сворачивается или разворачивается.
- Изначальное состояние свернутости определяется через props `collapsed`.

### Props

| Prop         | Required | Type                   | Default                         | Description                                                         |
|--------------|----------|------------------------|---------------------------------|---------------------------------------------------------------------|
| `header`     | -        | `string`               | `undefined`                     | Текст заголовка панели.                                             |
| `toggleable` | -        | `boolean`              | `false`                         | Определяет, можно ли сворачивать и разворачивать содержимое панели. |
| `collapsed`  | -        | `boolean`              | `false`                         | Определяет начальное состояние содержимого панели.                  |
| `cssClass`   | -        | `string`               | `EPanelPropsDefault.CSS_CLASS`  | Переопределяет структуру CSS классов.                               |
| `modifier`   | -        | `TSharedPropsModifier` | `undefined`                     | Модификатор базового CSS-класса.                                    |

### Slots

| Slot           | Description                                                                       |
|----------------|-----------------------------------------------------------------------------------|
| `default`      | Используется для кастомизации содержимого панели.                                 |
| `header`       | Используется для кастомизации заголовка панели.                                   |
| `icons`        | Используется для добавления иконок в заголовке панели.                            |
| `toggleButton` | Используется для кастомизации кнопки сворачивания или разворачивания.             |
| `toggleIcon`   | Используется для кастомизации содержимого кнопки сворачивания или разворачивания. |
| `footer`       | Используется для кастомизации подвала панели.                                     |

### Emits

| Event    | Payload                       | Description                                                                 |
|----------|-------------------------------|-----------------------------------------------------------------------------|
| `toggle` | `payload: TPanelToggleEvent`  | Событие срабатывает при сворачивании или разворачивании содержимого панели. |

Эмитов нет.

### Exposes

| Prop      | Type                                  | Description                                                   |
|-----------|---------------------------------------|---------------------------------------------------------------|
| `toggle`  | `(event: TPanelToggleEvent) => void`  | Метод для сворачивания или разворачивания содержимого панели. |

### Поддержка клавиатуры

| Key             | Function                                                                                   |
|-----------------|--------------------------------------------------------------------------------------------|
| `tab`           | Перемещает фокус на следующий фокусируемый элемент в последовательности вкладок страницы.  |
| `shift` + `tab` | Перемещает фокус на предыдущий фокусируемый элемент в последовательности вкладок страницы. |
| `enter`         | Переключает видимость основного содержимого.                                               |
| `space`         | Переключает видимость основного содержимого.                                               |

## Требования к тестированию:

- Покрыть компонент unit-тестами.

## Требования к документации

- Типы должны находится в отдельном файле `usePanel.d.ts`;
- Добавить TSDoc-комментарии к типам, компоненту и composable.
- Описание сценариев применения компонента.
