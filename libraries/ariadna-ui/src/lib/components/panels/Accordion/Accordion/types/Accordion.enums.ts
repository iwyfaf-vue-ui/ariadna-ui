/**
 * Accordion config.
 */
export enum EAccordionConfig {
  NAME = 'Accordion',
}

/**
 * Accordion default props values.
 */
export enum EAccordionPropsDefault {
  CSS_CLASS = 'ar-accordion',
}

/**
 * Accordion errors.
 */
export enum EAccordionErrors {
  OPENED_SINGLE_MODE_CONFLICT = 'The opened and singleMode props cannot be used together.',
}
