/**
 * Timeline config.
 */
export enum ETimelineConfig {
  NAME = 'Timeline',
}

/**
 * Timeline default props values.
 */
export enum ETimelinePropsDefault {
  KEY_PROPERTY = 'name',
  CSS_CLASS = 'ar-timeline',
}

/**
 * Timeline errors.
 */
export enum ETimelineErrors {
  KEY_PROPERTY_ERROR = 'Events (props.events) must contain the specified KeyProperty - ',
}
