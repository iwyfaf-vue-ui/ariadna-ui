Компонент поддерживает SSR, но написан на ESM, так что для его работы, необходимо транспилировать
библиотеку с помощью Babel:

**nuxt.config.ts**

```typescript
build: {
  transpile: ['@iwyfaf-vue-ui/ariadna-ui'];
}
```

В остальном, компонент работает как в обычном Vue 3 приложении.